export type { Database, Json } from "@/types/supabase";

// Domain type modules are added alongside each feature (auth, curriculum,
// assignments, assessments, portfolio, admin, billing) as it's built.
